import React from "react";
import { Link } from "react-router-dom";
import ProductDeets from "./ProductDeets";

const Products = ({
  products,
  cartItems,
  createLineItem,
  updateLineItem,
  auth,
}) => {
  return (
    <main className="ProductsContainerMain"></main>
  );
};

export default Products;
